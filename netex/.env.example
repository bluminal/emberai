# Write Operations (default: false)
NETEX_WRITE_ENABLED=false  # Set to "true" to enable cross-vendor write operations

# Cache (optional)
NETEX_CACHE_TTL=300        # Override default TTL in seconds
